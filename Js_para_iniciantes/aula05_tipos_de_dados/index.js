const nome = 'Andeson'; // string
const nome1 = "Andeson"; // string
const nome2 = `Andeson`; // string
const num1 = 10; // number
const num2 = 10.560; // number
let nomeAluno; //Quando se declara a variável ela recebe um valor indefinido, não apontando para nenhum local da memória
let sobrenomeAluno = null; // Nulo, também não aponta para nenhum local na memória
//NULL # Undefined
const boolean = true; // Lógicos, só pode ser true ou false

//para ver o type basta utilizar typeof

console.log(typeof nomeAluno);